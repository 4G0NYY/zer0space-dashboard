# Runtime dependencies. Pinned exactly so a rebuild of the same commit produces
# the same image — the alternative is a working deploy on Monday and a broken one
# on Friday for reasons that are not in the diff.
#
# Every one of these ships musllinux wheels, which is what makes the Alpine base
# image in the Dockerfile work without a compiler. If a future bump drops that,
# the fix is to switch the base image to python:3.12-slim rather than to add
# gcc/musl-dev to the runtime image.

fastapi==0.115.6
uvicorn[standard]==0.34.0
asyncpg==0.30.0
bcrypt==4.2.1
cryptography==44.0.0
httpx==0.28.1
itsdangerous==2.2.0
jinja2==3.1.5

# TOTP 2FA: pyotp generates/verifies codes, qrcode + Pillow render the setup QR
# as a PNG. All three are pure Python or ship musllinux wheels, so this stays
# compatible with the compiler-free Alpine image (see the note above).
pyotp==2.9.0
qrcode==7.4.2
pillow==11.1.0
