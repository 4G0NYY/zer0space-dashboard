{
  "name": "zer0space-dashboard",
  "version": "3.0.0",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "unlock-user": "node scripts/unlock-user.js",
    "migrate:sqlite": "node scripts/migrate-sqlite-to-pg.js"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "express": "^4.19.2",
    "express-session": "^1.18.0",
    "helmet": "^8.0.0",
    "multer": "^1.4.5-lts.1",
    "pg": "^8.13.1"
  },
  "//devDependencies": "better-sqlite3 is only needed by scripts/migrate-sqlite-to-pg.js. As a devDependency it is skipped by 'npm install --omit=dev' in the Dockerfile, so the runtime image needs no native build toolchain.",
  "devDependencies": {
    "better-sqlite3": "^9.6.0"
  }
}
